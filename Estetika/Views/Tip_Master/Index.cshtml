@model IEnumerable<Estetika.Tip_Master>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Imya_Tip_Master)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Imya_Tip_Master)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Tip_Master }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Tip_Master }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Tip_Master })
        </td>
    </tr>
}

</table>
