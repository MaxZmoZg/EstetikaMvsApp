@model Estetika.Models.Entities.Vopros

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Изменить вопросы к анализу</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID_Vopros)

    <div class="form-group">
        <label class="control-label col-md-2">Содержание вопроса</label>
        
        <div class="col-md-10">
            @Html.EditorFor(model => model.Zagolovok, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Zagolovok, "", new { @class = "text-danger" })
        </div>
    </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Сохранить" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Вернутсься", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
